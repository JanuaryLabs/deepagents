---
title: Overview
description: Convert natural language to SQL queries with AI-powered understanding of your database schema
---

Text2SQL converts natural language questions into SQL queries. It understands
your database schema, carries conversation state through your context store, and
can stream validated query results back to the user.

## Key Features

- **Natural Language to SQL**: Ask questions in plain English, get executable SQL
- **Schema-Aware**: Automatically analyzes your database structure, relationships, and data patterns
- **Multi-Database Support**: Works with PostgreSQL, SQLite, SQL Server, MySQL/MariaDB, and BigQuery
- **Teachable**: Add domain knowledge through context fragments and low-level SQL generation fragments
- **Conversation Memory**: Multi-turn chat backed by your `ContextEngine` store
- **Safe by Default**: Generates read-only queries with output limits

## Quick Example

```typescript
import { DatabaseSync } from 'node:sqlite';
import { groq } from '@ai-sdk/groq';
import { InMemoryFs } from 'just-bash';

import { ContextEngine, InMemoryContextStore } from '@deepagents/context';
import { Text2Sql } from '@deepagents/text2sql';
import { Sqlite } from '@deepagents/text2sql/sqlite';
import * as sqlite from '@deepagents/text2sql/sqlite';

const db = new DatabaseSync('./chinook.db', { readOnly: true });
const store = new InMemoryContextStore();

const text2sql = new Text2Sql({
  version: 'v1', // Bump this when your schema changes
  model: groq('openai/gpt-oss-20b'),
  filesystem: new InMemoryFs(),
  adapter: new Sqlite({
    execute: (sql) => db.prepare(sql).all(),
    grounding: [sqlite.tables(), sqlite.info()],
  }),
  context: (...fragments) => {
    const engine = new ContextEngine({
      store,
      chatId: 'chinook-demo',
      userId: 'demo-user',
    });
    engine.set(...fragments);
    return engine;
  },
});

// Generate SQL from natural language
const sql = await text2sql.toSql('Show me top 10 customers by total purchases');
// SELECT c.CustomerId, c.FirstName, c.LastName, SUM(i.Total) as TotalPurchases
// FROM Customer c JOIN Invoice i ON c.CustomerId = i.CustomerId
// GROUP BY c.CustomerId ORDER BY TotalPurchases DESC LIMIT 10
```

## Core Methods

| Method                    | Description                                            |
| ------------------------- | ------------------------------------------------------ |
| `toSql(input)`            | Generate SQL without executing                         |
| `chat(messages)`          | Run a multi-turn conversation using your context store |
| `index()`                 | Introspect database schema (cached)                    |
| `toPairs(factory)`        | Generate training pairs from the configured adapter    |

## When to Use Text2SQL

- Building analytics dashboards with natural language queries
- Creating chatbots that answer data questions
- Enabling non-technical users to query databases
- Prototyping data exploration interfaces

## Architecture

```
User Question
     │
     ▼
┌─────────────────┐
│    Grounding    │ ← Schema introspection (tables, columns, relationships)
└─────────────────┘
     │
     ▼
┌─────────────────┐
│ SQL Policies    │ ← Built-in SQL guidance + optional context fragments
└─────────────────┘
     │
     ▼
┌─────────────────┐
│  Text2SQL Agent │ ← Step-back prompting + validation + execution
└─────────────────┘
     │
     ├─────────────────────┐
     │                     │
     ▼                     ▼
┌─────────────┐   ┌──────────────┐
│ SQL Output  │   │ ContextStore │
│  / Stream   │   │ Persistence  │
└─────────────┘   └──────────────┘
     │
     ▼
   Results
```

## Next Steps

- [Getting Started](/docs/text2sql/getting-started) - Installation and first query
- [PostgreSQL Adapter](/docs/text2sql/postgresql) - PostgreSQL-specific setup
- [MySQL / MariaDB Adapter](/docs/text2sql/mysql) - MySQL and MariaDB-specific setup
- [BigQuery Adapter](/docs/text2sql/bigquery) - BigQuery-specific setup
- [Teach the System](/docs/text2sql/teach-the-system) - Add domain knowledge
